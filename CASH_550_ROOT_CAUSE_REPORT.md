# تقرير التحقيق الجنائي: لغز الرقم 550 في السيولة (Cash Root Cause)

## 1. مصدر الرقم 550 بالضبط
بعد إجراء مسح شامل لجميع ملفات المشروع (Codebase) وقواعد البيانات، تم التأكد بشكل قاطع من أن الرقم **550 ليس رقماً ثابتاً (Hardcoded)** ولا قيمة افتراضية (Default Value). 

**الرقم 550 هو الناتج الرياضي الدقيق لجدول `cash_transactions` داخل قاعدة بيانات التطوير المعزولة (Dev Database).**

### الإثبات بالأرقام (من داخل `maintenance_app_dev/maintenance.db`):
عند فحص قاعدة بيانات التطوير الخاصة بك، وجدنا العمليات النقدية التالية المسجلة حتى تاريخ 18/08/2026:
* **إجمالي المقبوضات النقدية (CUSTOMER_PAYMENT):** 561 دينار/دولار.
  *(100 + 16 + 110 + 30 + 20 + 30 + 30 + 40 + 75 + 10 + 100 = 561)*
* **إجمالي سحوبات الفنيين (TECHNICIAN_PAYMENT):** 11 دينار/دولار.
  *(6 + 5 = 11)*
* **السيولة الحالية (Cash Box) = 561 - 11 = 550 بالضبط.**

## 2. سبب "ثبات" الرقم وعدم استجابته للعمليات
السبب الجذري لعدم تغير الرقم رغم قيامك بإضافة عمليات وسحوبات هو **انفصال قواعد البيانات (Database Isolation)**، وهو نظام حماية قمنا بتطبيقه سابقاً لمنع تدمير بياناتك الحقيقية أثناء التطوير.

1. **التطبيق المستقر (Production):** يقرأ ويكتب في المسار الأصلي:
   `%APPDATA%\Maintenance App\maintenance.db`
2. **تطبيق التطوير (Development):** تم توجيهه في ملف `electron/main.ts` (السطر 7) ليقرأ ويكتب في مسار معزول تماماً:
   `%APPDATA%\maintenance_app_dev\maintenance.db`

عندما تقوم بفتح نسخة التطوير (`npm run dev`)، فإن الواجهة الرئيسية (Dashboard) تقرأ الرقم **550** من قاعدة البيانات المعزولة (التي تحتوي على نسخة قديمة من بياناتك توقفت عند 18/08).
وأي عمليات جديدة تقوم بإضافتها في **النسخة المستقرة (الإنتاج)** لن تظهر أبداً في **نسخة التطوير**، مما يجعلك تعتقد أن الرقم معلق (Stuck) برمجياً، بينما هو في الحقيقة يقرأ من قاعدة بيانات أخرى!

## 3. مسار تدفق البيانات (Data Flow)
`SQLite (maintenance_app_dev) -> statsRepo.ts -> IPC ('get-dashboard-stats') -> preload.ts -> React Dashboard (CashBox)`

* الواجهة لا تقوم بأي حسابات (No Double Counting). الواجهة فقط تعرض الرقم القادم من `statsRepo.ts`.
* `statsRepo.ts` يعتمد كلياً على جدول `cash_transactions` وهو جدول دفتر أستاذ (Ledger) دقيق جداً لا يتأثر بالديون غير المسددة.

## 4. إثبات صحة الحسابات المالية (Reconciliation Tests)
بناءً على طلبك، قمت بإنشاء سكريبت اختبار آلي شامل `test_cash_dashboard_reconciliation.ts` يقوم باختبار كامل لدورة حياة الأموال على قاعدة بيانات معزولة (Fresh DB).

**النتائج (جميعها PASS 100%):**
1. **رصيد افتتاحي 0** -> الكاش: 0
2. **إضافة عملية نقدية 10,000** -> الكاش: 10,000
3. **إضافة سحب 2,000** -> الكاش: 8,000
4. **إضافة عملية نقدية 5,000** -> الكاش: 13,000
5. **حذف السحب (2,000)** -> الكاش: 15,000
6. **عملية جزئية (دفع 5,000 ودين 15,000)** -> الكاش يرتفع فقط بـ 5,000 ليصبح: 20,000.
7. **تعديل سعر العملية النقدية إلى 10,000** -> الكاش: 25,000.
8. **تعديل السعر نزولاً إلى 5,000** -> الكاش ينقص ليعود: 20,000.
9. **تسديد دين كامل (15,000)** -> الكاش: 35,000.
10. **حذف عملية (10,000)** -> الكاش: 25,000.
11. **إغلاق الشهر (ترحيل 25,000)** -> الكاش في الشهر الجديد: 25,000 (رأس مال).
12. **عملية في الشهر الجديد (1,000)** -> الكاش: 26,000.
13. **Restore / استعادة نسخة احتياطية** -> الكاش يبقى 26,000 دون تلف.

**الخلاصة من الاختبار:** طبقة `statsRepo` والـ Ledger (`cash_transactions`) تعملان بتزامن رياضي مثالي. لا يوجد أي خطأ في الحسابات، ولا يوجد تكرار (Double Counting). 

## 5. حالة المشروع الحالية (Build Status)
تم تنفيذ جميع أوامر الفحص كما طلبت:
* `npm run lint`: **PASS** (لا توجد أخطاء)
* `npx tsc --noEmit`: **PASS** (لا توجد أخطاء Types)
* `npm run build`: **PASS** (البناء جاهز)
* `test_cash_dashboard_reconciliation.ts`: **PASS**

## 6. القرار النهائي لبناء الـ EXE
**نعم، المشروع الآن جاهز 100% لبناء نسخة Production (EXE) جديدة.**
لا يوجد أي Bug مالي حقيقي في نسخة التطوير (V2) الحالية. المشكلة التي واجهتها (550) هي مجرد نتيجة ثانوية صحيحة لبيئة التطوير المعزولة. النظام المالي الجديد (Cash Transactions Ledger) أثبت كفاءته في معالجة الديون، التعديل، والحذف بشكل مثالي.

للبدء في إصدار النسخة النهائية، يمكننا تنفيذ:
`npm run build:windows`
